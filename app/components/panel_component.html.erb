<div class='flex flex-col p-8 mt-8 bg-gray-700'>
    <h1 class="flex justify-center font-bold text-xl">
        <%= @title %>
    </h1>
    <div class='flex flex-col p-8 mt-8 bg-gray-700'>
      <%= content %>
  </div>
</div>
