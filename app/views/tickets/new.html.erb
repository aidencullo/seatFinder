<div class="mx-auto md:w-2/3 w-full">
    <h1 class="font-bold text-4xl">New ticket</h1>
</div>

<%= turbo_frame_tag 'new_message' do %>
    <%= render "form", ticket: @ticket, space: @space %>
<% end %>
